# @package _global_

defaults:
  - orbit_jet_puppi_ak8_ggHbb_minbias
  - override /model: model_faiss_kmeans_baseline
  - override /logger: wandb.yaml
  - _self_

task_name: orbit_particle_ggHbb_minbias_faiss_kmeans
tags: ["orbit", "particle", "ggHbb", "minbias", "faiss", "kmeans", "baseline"]

train: false
test: true
test_without_checkpoint: true

logger:
  wandb:
    project: orbit-tokenizer
    group: orbit_faiss_kmeans_baseline
    name: ${task_name}

model:
  model_kwargs: null
