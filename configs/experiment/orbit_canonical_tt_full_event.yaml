# @package _global_

defaults:
  - orbit_canonical_tt
  - _self_

project_name: orbit-canonical-tt-full-event
task_name: orbit_canonical_tt_full_event
tags: [orbit, canonical, tt, particle-full, full-event]

data:
  sequence_type: particle_full
  max_sequence_length: 500
  batch_size: 16

model:
  model_kwargs:
    max_sequence_len: 500

trainer:
  accumulate_grad_batches: 16

