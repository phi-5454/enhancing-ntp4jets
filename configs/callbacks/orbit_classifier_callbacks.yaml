defaults:
  - model_checkpoint.yaml
  - early_stopping.yaml
  - _self_

model_checkpoint:
  monitor: val_macro_auroc
  mode: max
  save_top_k: 1
  filename: "orbit_classifier_epoch_{epoch:03d}"

early_stopping:
  monitor: val_macro_auroc
  mode: max
  patience: 4
  min_delta: 0.0001
  strict: true
