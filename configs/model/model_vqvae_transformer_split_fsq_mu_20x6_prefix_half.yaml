defaults:
  - model_vqvae_transformer_split_fsq_mu_20x6
  - _self_

# Quantize only the leading ceil(N / 2) encoder latents. The input is pT ordered.
model_kwargs:
  latent_sequence_compression:
    enabled: true
    ratio: 0.5
    min_tokens: 1
    rounding: ceil
    mode: direct_prefix_masking
